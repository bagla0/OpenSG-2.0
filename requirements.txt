numpy
numba
meshio
# GPU (CUDA 12) by default -- falls back to CPU where no GPU exists.
# Lean CPU-only alternative: replace the two lines with plain `jax`.
jax[cuda12]
cupy-cuda12x
jaxopt
pyamg
libigl
matplotlib
mpi4py
fenics-basix
psutil
scipy
flax
# Intel MKL Pardiso solver (the direct default)
pypardiso
# PETSc GAMG solver (--solver iter 4).  pip BUILDS PETSc from source
# (~30-40 min); prefer `conda install -c conda-forge petsc4py` (the
# environment.yml route) where conda exists
petsc4py
